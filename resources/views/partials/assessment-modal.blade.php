<div id="assessmentModal" class="hidden fixed inset-0 bg-black/60 backdrop-blur-sm flex items-center justify-center z-50">
    <div
        class="bg-white dark:bg-gray-900 rounded-2xl shadow-2xl w-full max-w-3xl max-h-[90vh] overflow-y-auto p-6 relative">
        <button onclick="closeAssessmentModal()" class="absolute top-3 right-3 text-gray-500 hover:text-gray-800">
            ✕
        </button>
        <div id="assessmentContent" class="text-gray-900 dark:text-gray-100">
            <p class="text-center text-gray-400">Carregando avaliação...</p>
        </div>
    </div>
</div>
